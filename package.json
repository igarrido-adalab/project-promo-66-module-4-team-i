{
  "name": "project-promo-66-module-4-team-i",
  "version": "0.0.1",
  "description": "Servidor de proyectos molones",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "deploy": "rm -rf public ; cd FRONTEND_REACT/ && npm run build && mv docs ../public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
