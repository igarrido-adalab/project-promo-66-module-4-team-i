## 1. Setup & Atomic Components

- [x] 1.1 Create `src/components/` directory
- [x] 1.2 Create `Button` component in `src/components/Button.jsx`
- [x] 1.3 Create `Heading` component in `src/components/Heading.jsx`

## 2. Layout Components

- [x] 2.1 Extract `Header` into `src/components/Header.jsx`
- [x] 2.2 Extract `Footer` into `src/components/Footer.jsx`

## 3. Data & Functional Components

- [x] 3.1 Extract `Card` into `src/components/Card.jsx`
- [x] 3.2 Extract `Preview` into `src/components/Preview.jsx` (utilizing `Card`)
- [x] 3.3 Extract `Form` into `src/components/Form.jsx` (utilizing `Button` and `Heading`)

## 4. Page Orchestration

- [x] 4.1 Create `CreatePage` in `src/components/CreatePage.jsx` to wrap `Preview` and `Form`
- [x] 4.2 Refactor `App.jsx` to use the new components and serve as the main container

## 5. Validation

- [x] 5.1 Verify that all components are correctly rendering and receiving props
- [x] 5.2 Verify that live preview functionality still works as expected
- [x] 5.3 Run a final check on the layout to ensure no styling regressions
